# Changelog

## Unreleased

## 0.1.2 — 2026-08-04

- Resolve Notes Apple Events by bundle identifier so commands work when macOS cannot resolve the `Notes` display name.
- Add explicit `--read-only` mode for assistant lookups; it blocks folder and note writes and deletes before invoking a write backend.

## 0.1.1 — 2026-06-17

- Initial release.
